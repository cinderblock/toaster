{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to remote toaster app",

      // We use SSH port forwarding so that this configuration is universal
      "address": "localhost",
      "port": 9229,

      "smartStep": true,

      "sourceMaps": true,

      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/home/pi/toaster",

      // "skipFiles": [
      //   "<node_internals>/**"
      // ],
      "outFiles": [
        // "${workspaceFolder}/.build/**/*",
        "${workspaceFolder}/src/**/*"
      ]
    }
  ]
}
